version: "1"
workflows:
  - name: example-workflow
    description: example workflow that echoes a message to the terminal
    remote: false
    public: false
    sourceCodeURL: ""
    steps:
      - echo hello 1
      - echo hello 2
      - echo hello 3
    env:
      - USER=world
      - OPS_ACCESS_TOKEN
